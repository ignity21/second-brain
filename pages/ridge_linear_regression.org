:PROPERTIES:
:ID:       2345ac0f-e2ff-4562-9ef4-be441fc59cd5
:END:
#+TITLE: Ridge Linear Regression
#+filetags: :ML:

- tags :: [[id:46646008-096b-4086-beab-62c2dd5f5825][Linear Regression]]


The formula to make predictions is the same one used for [[id:5a859956-25b8-42cb-9faf-5a67e61061b4][OLS Linear Regression]].
The Ridge model makes a trade-off between the simplicity of the model (near-zero coefficients)
and its performance on the training set.

- The coefficients /w/ are chosen not only so that they predict well on the training data,
  but also to fit an additional constraint.
- Ridge is a more restricted model.
- Constraint: The magnitude of coefficients should be as small as possible. (known as L2 regularization)
  - Means each feature should have as little effect on the outcome as possible.
  - It's an example of [[id:483b6d38-f2bc-4879-84ab-27e0a76561fa][Regularization]]
  - Ridge penalizes the L2 norm of the coefficients /w/.
- Given enough data, ridge and linear regression will have the same performance

* Code
  #+begin_src python
    from sklearn.linear_model import Ridge

    ridge = Ridge(alpha=2).fit(X_train, y_train)
    print("Training set score: {:.2f}".format(ridge.score(X_train, y_train)))
    print("Test set score: {:.2f}".format(ridge.score(X_test, y_test)))
  #+end_src

* Parameters
- *alpha*
  - Increasing alpha forces coefficients to move more toward zero, which decreases
    training set performance but might help generalization.
  - A higher alpha means a more restricted model.
  - The optimum setting of alpha depends on the particular dataset we are using.

* Comparison with Linear Regression
[[../assets/ML/Ridge_1.png]]
