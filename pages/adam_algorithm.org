:PROPERTIES:
:ID:       aaa2de75-efd7-4439-a2dd-18a4c221a716
:END:
#+TITLE: Adam Algorithm
#+filetags: :ML:

- tags :: [[id:a14aae7c-868f-4724-8a0d-9ab233d01264][Optimization Algorithm]]


Adam: Adaptive Moment Estimation
- not just one learning rate $\alpha$
- adapt the learning rate automatically
[[../assets/ML/AdamAlgoIntuition]]

- If $w_j$ (or $b$) keeps moving in same direction, increase $\alpha_j$
- If $w_j$ (or $b$) keeps oscillating, reduce $\alpha_j$

* Code
#+begin_src python
  model = Sequential([...])
  model.compile(optimizer=tf.keras.optimizers.Adam(learning_rate=1e-3) # initial alpha
                loss=tf.keras.losses.SparseCategoricalCrossentropy(from_logits=True))
  model.fit(X, y, epochs=100)
 #+end_src
