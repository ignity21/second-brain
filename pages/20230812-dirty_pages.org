:PROPERTIES:
:ID:       7910f1b4-5228-4da4-97df-282313e576ac
:END:
#+title: Dirty Pages
#+filetags: Linux

- 读文件缓存可直接回收
- 脏页 (Dirty Pages) 指的是在写文件缓存中，还没写入硬盘的数据。需要先写入磁盘之后才能释放内存。

这些脏页，一般可以通过两种方式写入磁盘：

- 在应用程序中，通过系统调用 ~fsync()~ ，把脏页同步到磁盘中
- 或由系统内核线程 ~pdflush~ 负责这些脏页的刷新
