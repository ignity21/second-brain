:PROPERTIES:
:ID:       99384a8d-1cd7-45e6-9913-01a96e895630
:END:
#+title: NumPy
#+filetags: :DataScience:

* Filtering
** ~np.logical_and~, ~np.logical_or~
#+begin_src python
cpc = cars['cars_per_cap']
medium = cars[np.logical_and(cpc>=100, cpc<=500)]
#+end_src

* Loops
** Loops for ndarray
#+begin_src python
# For ND numpy array
for x in np.nditer(np_baseball):
    print(x)
#+end_src

* Random Generators
#+begin_src python
import numpy as np

np.random.seed(42)

np.random.rand() # 0~1

coin = np.random.randint(0, 2)
print(coin)
#+end_src
