:PROPERTIES:
:ID:       9512aa20-205e-4931-854a-21507cd60530
:END:
#+TITLE: C++ Hints
#+filetags: :C++:

* Data Type
** array
#+begin_src cpp
  int array[26] = {0};
#+end_src
** ~std::vector~
#+begin_src cpp
  vector<int> v1{1, 2, 3};
  vector<int> v2 = {4, 5};
  vector<int> v3;
  v3 = {6, 7, 8};

  vector<vector<int>> v2d = {{1, 2, 3}, {4, 5, 6}, {7, 8, 9}};
#+end_src

** ~istringstream~
Streaming data from a string
#+begin_src cpp
  string a("1 2 3");
  istringstream my_stream(a);

  int n;
  // The extraction operator will read until whitespace is reached
  my_stream >> n;
  // Testing to see if the stream was successful
  if (my_stream) {...};

  // mixed type
  string b("1,2,3");
  istringstream mix_stream(b);
  char c;
  int n;

  // ! notice that the 3 was not printed
  while (mix_stream >> n >> c) {
    cout << n << " " << c << ", ";
  }
#+end_src

** ~enum~
*** Scoped Enums
~enum + class/structure + name {items}~
#+begin_src cpp
  enum class Color {red, blue, yellow};
  Color my_color = Color::blue;
#+end_src
*** Unscoped Enums
~enum + name {items}~
#+begin_src cpp
  enum Color {red=0, blue, yellow};
  Color my_color = blue;
#+end_src
