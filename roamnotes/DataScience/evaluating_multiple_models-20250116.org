:PROPERTIES:
:ID:       5456bf47-a5cd-4413-86ac-09408f009fe0
:END:
#+title: Evaluating Multiple Models
#+filetags: :Python:ML:

* Principles
- Size of the dataset
  - Fewer features = simple model, faster training time
  - Some models require large amounts of data to perform well, e.g. [[id:31b71a06-0592-444b-b410-916ee717b488][Neural Network]]
- Interpretability
  - Some models are easier to explain, can be important for stakeholders
    - e.g. [[id:b6f03e6b-8850-4f2d-8d2d-f337029fe1e7][Linear Regression]] the model coefficients can be interpret
- Flexibility
  - May improve accuracy, by making fewer assumptions about data
  - [[id:4dec577e-c1d9-4b01-9a95-63e3413b42a9][KNN]] is a more flexible model, doesn't assume any linear relationship.

* Comparing Different Models
- [[id:8038235d-fdb1-44b6-b2b9-76c38e740f0e][Regression Metrics]]: RMSE, R-squared
- [[id:91a5b0fd-48cd-47aa-8fff-cb9cb0fd520c][Classification Metrics]]
  - Accuracy, Confusion Matrix
  - [[id:21f0b9c3-bda5-47cd-82e2-a885400f71c1][Precision and Recall]], F1-score, ROC AUC
- Best to [[id:8dc25af2-f3bd-4ec4-b949-40958e167e75][scale data]] before evaluating models
#+begin_src python
import matplotlib.pyplot as plt
from sklearn.preprocessing import StandardScaler
from sklearn.model_selection import train_test_split, cross_val_score, KFold
from sklearn.neighbors import KNeighborsClassifier
from sklearn.linear_model import LogisticRegression
from sklearn.tree import DecisionTreeClassifier

X = music.drop("genre", axis=1).values
y = music["genre"].values
X_train, X_test, y_train, y_test = train_test_split(X, y, random_state=42)
scaler = StandardScaler()
X_train_scaled = scaler.fit_transform(X_train)
X_test_scaled = scaler.transform(X_test)

models = {
    "KNN": KNeighborsClassifier(),
    "Logistic Regression": LogisticRegression(),
    "Decision Tree": DecisionTreeClassifier(),
}

results = []
for model in models.values():
    kfold = KFold(n_splits=6, random_state=42, shuffle=True)
    cv_results = cross_val_score(model, X_train_scaled, y_train, cv=kfold, scoring="accuracy")
    results.append(cv_results)

plt.boxplot(results, labels=models.keys())
plt.show()

# Test set performance
for name, model in models.items():
    model.fit(X_train_scaled, y_train)
    test_score = model.score(X_test_scaled, y_test)
    print(f"{name} test set accuracy: {test_score:.2f}")
#+end_src
