:PROPERTIES:
:ID:       cecdb0e0-82ac-467f-8585-56c966db4551
:END:
#+title: Array
#+filetags: :C++:

* Overview
#+begin_src cpp
float weight[5] = {0.1, 0.2}; // partial initialization
float weight[5] = {0}; // all elements initialized to 0
char hello[] = {'H', 'e', 'l', 'l', 'o', '\0'}; // can be used as a string
char hello_notstr[] = {'H', 'e', 'l', 'l', 'o'}; // no null terminator

char c;
while(c=hello[i++]) // loop until null terminator
    putchar(c);

for(int i=0; i<sizeof(hello); i++) // loop until null terminator
    putchar(hello[i]);

// Compare strings
char hello2[] = "Hello";
if(strcmp(hello, hello2) == 0)
    puts("Strings are equal");

// Dynamic memory allocation
int *parray = new int[10];
delete[] parray;
#+end_src

* Pointer to Array
:PROPERTIES:
:ID:       66214d1f-a18e-4d7b-bf7e-f796148c6c18
:END:
#+begin_src cpp
#include <iostream>
int nums[] = {1, 2, 3, 4, 5};
int *ptr= nums; // Pointer to the first element of the array
// &ptr: address of the pointer itself
// ptr: first element address
// nums: address of the first element
// *ptr: first element value
// ptr[0]: first element value
// ptr[1]: second element value
// *(ptr++): increment pointer to the second and return the first element value
#+end_src
