:PROPERTIES:
:ID:       03c58b55-8c1b-47c1-a48a-bf1d428476a4
:END:
#+title: Emacs

- links :: [[id:1d0af911-edab-4f6e-8233-d71b99ae78e8][Emacs Lisp]]

* Variables
- ~setq~
- ~setq-default~ sets values only in buffers that do not have their own local values for the variable.
- ~defvar~
- ~defcustom~

* Customizable Variables
** ~defcustom~
Arguments
1. variable name
2. initial value, this value is set only if the value has not already been set
3. documentation
4. types, options and group (not featured in ~defvar~)
#+begin_src elisp
(defcustom text-mode-hook nil
  "Normal hook run when entering Text mode and many related modes."
  :type 'hook
  :options '(turn-on-auto-fill flyspell-mode)
  :group 'wp)
#+end_src
- The ~:type~ keyword tells Emacs the kind of data to which text-mode-hook should be set
- The ~:options~ keyword specifies a suggested list of values for the variable

** Customize It
1. ~M-x customize~
2. ~custom-set-variables~

* Keybindings
- ~keymap-global-set~ set global bindings
  - example: ~(keymap-global-set "C-z" 'shell)~
  - low level function: ~(global-set-key (kbd "C-z") 'shell)~
- ~keymap-set~
  - ~(keymap-set texinfo-mode-map "C-c p" 'backward-paragraph)~
  - low level: ~(define-key texinfo-mode-map (kbd "C-c p") 'backward-paragraph)~
- Reserved key sequences for users (should not be used by modes)
  - C-c followed by a letter
  - Function keys F5 through F9 without modifiers

* Autoloading
#+begin_src elisp
(autoload 'html-helper-mode
  "html-helper-mode" "Edit HTML documents" t)
#+end_src
- This expression autoloads the ~html-helper-mode~ function.
- It takes it from the ==html-helper-mode.el/elc= file
  - The file must be located in a directory specified by load-path
- interactive -> t

* [[id:e28fe170-8733-448f-9911-2f785b901549][Debugging]]
