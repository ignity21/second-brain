# 知识库协作约定

本仓库以 `roamnotes-v2/` 为 org-roam 笔记库，`roamnotes/` 为旧版迁移来源，`ankinotes/` 存放 Anki 卡片。

## 工作范围与完成标准

- 按任务读取相关笔记、索引和说明；普通文字修改无需预读全库或执行迁移检查。
- 在用户请求范围内完成编辑、必要的索引和链接更新、相关验证，并修复本次改动引入的问题，无需在每一步重复确认。发现无关历史问题时说明即可。
- 保留用户已有改动。`pages/`、`logseq/` 是 Logseq 专用目录，不改动。
- 多轮或大型改动前，在 `.agents/tasks/` 建立任务文件（Markdown 检查清单 + 状态），进度记录写入该文件；不使用工具私有的 todo 存储（如 `.pi/`）。
- 按需阅读：迁移旧笔记时读 [迁移说明](docs/note-migration.md)；创建或修改 Anki 卡片时读 [Anki 规则](docs/anki.md)。

## org-roam 文件与节点

- 大写字母开头的目录表示大类；未分类笔记放 `Inbox/`。大类内平铺笔记和索引文件，仅图片、示例代码等资源可有子目录，不按 topic 建笔记子目录。
- 各大类的图片放本类 `images/`，共享资源放笔记库根目录 `assets/`。资源链接使用 `[[file:...]]`，相对路径以引用文件所在目录为基准。
- 笔记间引用使用 `[[id:...][Title]]`，不用文件链接。笔记创建后，将对应的纯文本占位替换为 ID 链接。
- 笔记文件的 `:ID:` 使用 UUID v4；新 ID 用 `uuid.uuid4()` 随机生成，不使用人为可读 ID。节点语义未变时，移动、改标题或调整层级都保留原 ID。遇到旧 ID 不合规范时，不在无关编辑中直接重建身份。
- 标题尽量用英语；`#+title` 带足领域上下文，避免全库出现含义不清的通用标题或不必要的重名。索引链接描述可简短，例如节点标题为 `Emacs Lisp Core Language`，链接描述为 `Core Language`。
- 中文别名写入文件的 `:PROPERTIES:` 抽屉中的 `:ROAM_ALIASES:`。

## 主索引树

这些规则适用于新增、迁移和重组节点；正文中的知识引用和反链可自由形成网状关系。

- 每个大类有同名一级索引，如 `Python/python.org`。大类索引用 `* [[id:...][Group Name]]` 链接下一层索引，不直接收录具体笔记。
- 默认层级为「大类索引 → 中间索引 → 具体笔记」；需要子分组时最多四层。索引逐层连接，不绕过已有中间索引或子分组。
- 表示分类的分组建立独立、带 ID 的 org-roam 文件，与笔记一起平铺；不能用索引中的普通标题代替节点。索引可只有导航链接，普通标题用于单篇笔记的内容排版。
- 除 `Inbox/` 未分类笔记外，每个非根节点有一个主父节点，并能从大类根索引到达。跨领域笔记选择一个主归属，其他位置在正文中作语义引用，不重复收录为主索引入口。
- 单个索引建议连接 2–8 个下一层节点；超过 8 个评估拆分，超过 10 个原则上增加语义明确的中间节点。单子节点的中间索引通常合并，但稳定领域边界、资源类型或预期扩展的节点可保留。
- 新笔记加入合适的中间索引；必要时创建并接入新索引。只有形成稳定、独立的知识领域才新建大类，不为少量笔记建大类。资源也应由合适的中间索引收录。
- 删除或合并节点前，检查全库入链，更新有效引用或保留节点身份。

## Org 排版

- 普通笔记的一级内容标题超过 5 个时，以 `* TOC :TOC_1:` 作为首个一级标题；二级内容标题总数超过 5 个时优先使用 `* TOC :TOC_2:`。计数不含 TOC 本身；索引文件不另加 TOC。
- 中文与嵌入的英文单词或短语之间留一个半角空格；中文标点前不额外加空格，例如：`你说 he 是谁？他是 Jerry。`
- 中文修饰沿用仓库约定：`** 中文 **`、`~~ 中文 ~~`、`= 中文 =`、`~ 中文 ~`，标记与中文之间留空格。

## 按改动验证

- 内容编辑：检查受影响笔记的 TOC、Org block、property drawer 和链接语法。
- 新增或更改标题：在全库比较相关标题，确认领域上下文清晰且没有引入不必要的重名。
- 移动文件或调整资源引用：确认受影响的 `file:` 链接指向存在的文件，分类目录仍保持扁平。
- 新增节点、迁移或重组索引：检查受影响主索引树的逐层连接、根节点和中间索引出度、可达性、唯一主父节点，并检查全库对相关 ID 的引用。
- 完成文件移动或 ID 索引调整后，执行一次 `org-roam-db-sync`。若当前环境无法调用用户的 Emacs/org-roam，说明未完成的同步及原因，不把未执行的检查报告为通过。
- 完成时简述修改结果、实际验证和剩余问题。仅编辑本说明或其他 Markdown 文档时，检查文档链接与差异即可。
